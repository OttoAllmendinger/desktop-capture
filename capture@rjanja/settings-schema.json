{
   "camera-program": {
      "default": "cinnamon",
      "type": "combobox",
      "options": [
         "cinnamon",
         "gnome-screenshot"
      ],
      "description": "Preferred screen capture program"
   },
   "recorder-program": {
      "default": "cinnamon",
      "type": "combobox",
      "options": [
         "cinnamon",
         "gtk-recordmydesktop"
      ],
      "description": "Preferred screen recorder program"
   },
   "include-cursor": {
      "default": false,
      "type": "checkbox",
      "description": "Whether to include mouse cursor in screenshot"
   },
   "open-after": {
      "default": false,
      "type": "checkbox",
      "description": "Whether to open the screenshot in file manager"
   },
   "delay-seconds": {
      "default": 2,
      "type": "spinbutton",
      "min": 0,
      "max": 600,
      "units": 1,
      "step": 1,
      "description": "How many seconds to wait before taking a screenshot"
   },
   "camera-save-dir": {
      "default": "~/Pictures/screenshots",
      "type": "filechooser",
      "description": "Save location for screen grabs"
   },
   "recorder-save-dir": {
      "default": "~/Videos/screencasts",
      "type": "filechooser",
      "description": "Save location for screen recordings"
   },
   "camera-save-prefix": {
      "default": "screenshot-%TYPE-%Y-%M-%D-%H%I%S",
      "type": "entry",
      "description": "Filename format for screen grabs"
   },
   "recorder-save-prefix": {
      "default": "screencast-%Y-%M-%D-%H%I%S",
      "type": "entry",
      "description": "Filename format for screen recordings"
   },
   "capture-window-as-area": {
      "default": false,
      "type": "checkbox",
      "description": "Whether to use area selection for window captures"
   },
   "include-window-frame": {
      "default": true,
      "type": "checkbox",
      "description": "Whether to include the window frame and titlebar in window captures"
   },
   "use-camera-flash": {
      "default": true,
      "type": "checkbox",
      "description": "Whether to show a bright flash over the capture area"
   },
   "show-capture-timer": {
      "default": true,
      "type": "checkbox",
      "description": "Whether to show the timer on the screen"
   },
   "play-shutter-sound": {
      "default": true,
      "type": "checkbox",
      "description": "Whether to play the shutter sound as the capture is made"
   },
   "play-timer-interval-sound": {
      "default": true,
      "type": "checkbox",
      "description": "Whether to play the timer interval sound as the timer counts down"
   },
   "copy-to-clipboard": {
      "default": true,
      "type": "checkbox",
      "description": "Whether to copy path/filename of screenshots to clipboard after capture"
   },
   "upload-to-imgur": {
      "default": false,
      "type": "checkbox",
      "description": "Whether to upload all captures to imgur.com",
      "help": "If enabled, all captures will be uploaded to imgur. The view and delete URLs for each capture are stored in a file imgur.log in the capture save directory."
   },
   "send-notification": {
      "default": true,
      "type": "checkbox",
      "description": "Whether to send a notification to desktop after capture"
   },
   "include-styles": {
      "default": false,
      "type": "checkbox",
      "description": "Whether to include CSS selection styles in Cinnamon UI screenshots"
   },
   "mod-activates-timer": {
      "default": false,
      "type": "checkbox",
      "description": "Whether to only activate timer when Shift key is held"
   },
   "last-selected-page": {
      "default": 0,
      "type": "generic",
      "description": "Last selected page (tab index) of settings application"
   },
   "last-selected-subpage": {
      "default": 0,
      "type": "generic",
      "description": "Last selected sub-page (tab index) of settings application"
   },
   "use-symbolic-icon": {
      "default": true,
      "type": "checkbox",
      "description": "Whether to use symbolic (black and white) icon for applet"
   }
}